<% if (title) {%>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>

<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<div class="inventory-form">
    <form id="inventoryForm" action="./newInventory" method="post">
        <label for="className">Classification</label>
        <select name="classification_id" id="className" required value="<%= locals.classification_id %>">
            <option value="" disabled selected>Choose a Classification</option>
            <% classifications.forEach(classification => { %>
                <option value="<%= classification.classification_id %>"><%= classification.classification_name %></option>
            <% }) %>
        </select>
        <label for="invMaker">Make</label>
        <input type="text" minlength="3" name="inv_make" id="invMake" placeholder="Min of 3 characters" required value="<%= locals.inv_make %>">
        <label for="invModel">Model</label>
        <input type="text" minlength="3" name="inv_model" id="invModel" placeholder="Min of 3 characters" required value="<%= locals.inv_model %>">
        <label for="invDescription">Description</label>
        <textarea name="inv_description" id="invDescription" cols="30" rows="10" required value="<%= locals.inv_description %>"></textarea>
        <label for="invImg">Image Path</label>
        <input type="text" name="inv_image" id="invImg" required value="<%- locals.inv_image %>">
        <label for="invThumb">Thumbnail Path</label>
        <input type="text" name="inv_thumbnail" id="invThumb" required value="<%-locals.inv_thumbnail %>">
        <label for="invPrive">Price</label>
        <input type="number" step="any" name="inv_price" id="invPrice" placeholder="decimal or integer" required value="<%= locals.inv_price %>">
        <label for="invYear">Year</label>
        <input type="text" name="inv_year" id="invYear" pattern="^\d{4}$" title="Please enter a 4-digit year (e.g., 2024)" placeholder="4-digit year" required value="<%= locals.inv_year %>">
        <label for="invMiles">Miles</label>
        <input type="number" name="inv_miles" id="invMiles" placeholder="digits only" required value="<%= locals.inv_miles %>">
        <label for="invColor">Color</label>
        <input type="text" name="inv_color" id="invColor" required value="<%= locals.inv_color %>">

        <button type="submit" class="btn">Add Inventory</button>
    </form>
</div>